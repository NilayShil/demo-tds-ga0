name: My First GitHub Action

on:
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: 24f3002280@ds.study.iitm.ac.in
        run: echo "Hello from my GitHub Action!"
